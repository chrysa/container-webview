# Ce fichier est conservé pour référence historique (ancienne config GitLab CI).
# Le projet est désormais hébergé sur GitHub et utilise GitHub Actions.
# Voir .github/workflows/ pour les pipelines actifs.
